<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_chats">

    <fragment
        android:id="@+id/navigation_chats"
        android:name="com.example.enigma.ChatsFragment"
        android:label="Chats"
        tools:layout="@layout/fragment_chats">
    </fragment>

    <fragment
        android:id="@+id/navigation_contacts"
        android:name="com.example.enigma.ContactsFragment"
        android:label="Contacts"
        tools:layout="@layout/fragment_contacts" />

    <fragment
        android:id="@+id/navigation_add_contact"
        android:name="com.example.enigma.AddContactFragment"
        android:label="Add Contact"
        tools:layout="@layout/fragment_add_contact"/>

</navigation>